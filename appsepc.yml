version: 0.2

phases:
  install:
    commands:
      - node -v
      - npm -v

  build:
    commands:
      - ls -la
      - ls -la dist
      - aws s3 cp dist s3://ap-test-logs/angular-study/ --recursive

#files:
#  - source: /
#    destination: /root/destination/directory
#
#file_exists_behavior: OVERWRITE

